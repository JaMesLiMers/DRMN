# New runnable variant; not a verified historical experiment configuration.
seed: 0
cpu_threads: 2
model:
  num_stages: 3
  num_points: 100
  max_seg_num: 64
  max_sequence_length: 230
  encoder_layers: 3
training:
  learning_rate: 0.0001
  epochs: 20
