> 本轮范围更新：按用户要求不准备真实数据、不提供 checkpoint；代码审查最新结论见 最终一致性核对.md。以下保存既有验证与效果验证边界。

# 当前验收与剩余工作

用户约束：不重新进行完整训练；使用原权重进行推理/评测。只做小样本前向、损失和反向检查，不执行优化器更新。

## 已通过

- CPU 环境可安装、主模型可导入。
- 原始随机图片 + 真实分词文本 → 冻结编码器 → 四阶段 DRMN 掩码。
- 特征输入 → BCE + Dice → 有限梯度；参数未更新。
- 主模型 281、图像编码器 538、BERT 465 项参数及缓冲区与旧权重结构对应。
- 相同参考算子条件下，迁移前后主模型四阶段输出完全一致。
- 权重保存/加载预测一致、缺失参数拒绝加载。
- 独立评测与掩码导出 CLI 可运行（临时随机权重、合成数据）。
- 18 项单元与集成测试通过。

## 尚未通过，不能宣称效果等同论文

- 完好原权重：现有 checkpoint 有 424 个存储块 CRC 失败，其中主模型 46 个。
- 完整真实 PNG/COCO2017 数据及其划分、预处理的验证。
- 原 CUDA 算子与重建 PyTorch 参考实现的数值对照。
- 最佳实验采样点数等无权重形状信息可推断的配置。
- 原权重真实预测、整体 62.9% 及分组指标复核。

工程已从静态备份变成可运行的验证版本。它尚不是经过原权重和真实数据验收的论文复现发布版。未找到完好权重时不自动改为重训。
